# render an `aiScene`

This is a preparation to implement my `Impor3D` project.

This demo shows how to load and display an `*.obj` file(including `*.mtl` and textures) in legacy openGL.

![aiSceneTextureLegacy](aiSceneTextureLegacy.png)

